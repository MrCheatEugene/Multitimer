# Multitimer
A simple Timer JS library

# Usage
Include index.js, and add `data-timer="2025-11-28T20:00:00"` to any element, where time inside can be parsable by Date JS constructor. <br/>
You can add timers even after page loaded. Just do so without `data-id` attribute.
